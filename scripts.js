const countBtn = document.getElementById(count-Btn)
// i need to link 1 more variable to javascript

countBtn.addEventListener("click", goCounting)

function goCounting(){
    countBtn = "";

    function printHelloWorld(){
        // idk if I use button or count-Btn
        countBtn.innerHTML = "hello World"
    }
}